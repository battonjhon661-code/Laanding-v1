Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files and directories directly (CSS, JS, images, fonts)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback to index.html
RewriteRule ^ /index.html [L]
